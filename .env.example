DATABASE_URL="postgresql://taskuser:taskpass@db:5432/taskmanager?schema=public"
PORT=3000
# Note: if your password contains special characters (#, @, :, /, %, etc.),
# percent-encode them (e.g. # -> %23) or the connection string will parse incorrectly.
